#
# Ron DSPy agent runtime requirements.
#
# IMPORTANT:
# - Ron's "tool registry" imports `strands_tools` from the local `agents/tools/` package.
# - Installing it (editable) pulls in required deps like `rich` and `requests`.
#
# Install the local Strands tools package (contains `strands_tools` + deps).
# NOTE: pip resolves relative paths from your *current working directory*.
# Run installs from the repo root:
#   python -m pip install -r agents/Ron/requirements.txt
#
# Enable TwelveLabs tools (`chat_video`, `search_video`) by installing the
# `twelvelabs` optional extra from the local package.
-e ./agents/tools[twelvelabs]

dspy-ai>=2.4.0
pydantic>=2.0.0

# Optional (enables long-term memory integrations)
mem0ai>=0.1.99,<1.0.0

